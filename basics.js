const accountId = "123234345"
let accountName ="rajveer"
var state = "rajasthan"
accountNo = "123"
let stateDate 


/*
mainly we not use the var in the js 
because it will give the issue in the block scope {} and function scope fun{}
mainly it will can all the same var name data if we can at a sigle place also 
*/

// accountId = "raj234"   //this thing is not possible in const because we can declared it only once 
accountName = "raj"
state = "madhya pradesh"
accountNo ="321"

console.log(accountId)

console.table([accountId, accountName,state,accountNo,stateDate])